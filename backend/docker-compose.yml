services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: nightcord
      POSTGRES_PASSWORD: nightcord
      POSTGRES_DB: nightcord
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
